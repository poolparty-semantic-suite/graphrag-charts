{{- if .Values.serviceAccount.create -}}
{{- $saAnnotations := mergeOverwrite (deepCopy .Values.annotations) .Values.serviceAccount.annotations -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "graphrag-components.serviceAccountName" . }}
  namespace: {{ include "graphrag-components.namespace" . }}
  labels:
    {{- include "graphrag-components.labels" . | nindent 4 }}
  {{- with $saAnnotations }}
  annotations:
    {{- tpl (toYaml .) $ | nindent 4 }}
  {{- end }}
{{- end }}
