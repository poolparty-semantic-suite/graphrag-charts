{{- if .Values.serviceAccount.create -}}
{{- $saAnnotations := mergeOverwrite (deepCopy .Values.annotations) .Values.serviceAccount.annotations -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "graphrag-conversation.serviceAccountName" . }}
  namespace: {{ include "graphrag-conversation.namespace" . }}
  labels:
    {{- include "graphrag-conversation.labels" . | nindent 4 }}
  {{- with $saAnnotations }}
  annotations:
    {{- tpl (toYaml .) $ | nindent 4 }}
  {{- end }}
{{- end }}
