# Importing N8N entities

The Job [n8n-import.yaml](n8n-import.yaml) can be used as an example of how to import entities in n8n from a configmap.

It can be modified with additional containers that download the entities before that instead of using the configmap.
